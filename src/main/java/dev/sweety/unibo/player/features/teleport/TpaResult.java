package dev.sweety.unibo.player.features.teleport;

public enum TpaResult {
    SUCCESS,
    ALREADY_IN_TELEPORT,
    ALREADY_REQUESTED,
    TARGET_NOT_FOUND,
    NO_REQUEST,
    NOTHING_TO_CANCEL,
    CANCELLED
}
